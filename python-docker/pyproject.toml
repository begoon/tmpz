[project]
name = "python-docker"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "docker>=7.1.0",
    "pymongo>=4.16.0",
    "pytest>=9.0.2",
    "tenacity>=9.1.4",
]
