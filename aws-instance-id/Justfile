default: build

# Build sdist and wheel into dist/
build:
    uv build

# Publish to PyPI (requires UV_PUBLISH_TOKEN or ~/.pypirc credentials)
publish: clean build
    uv publish

run:
    uv run ec2-instance-id

clean:
    rm -rf dist .venv
