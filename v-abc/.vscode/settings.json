{
    "terminal.integrated.env.osx": {
        "PATH": "${HOME}/other/v:${PATH}"
    },
    "emeraldwalk.runonsave": {
        "commands": [
            {
                "match": "\\.v$",
                "cmd": "${HOME}/other/v/v fmt -w ${file}"
            }
        ]
    }
}