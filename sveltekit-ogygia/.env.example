# Optional. Signs the server-island endpoint URLs so a harvested URL cannot be replayed with
# other props. When unset, ogygia mints a random secret at build time (fine for a single
# process; set it explicitly when running several instances behind a load balancer).
# Must be at least 16 bytes for production builds.
OGYGIA_SECRET=
